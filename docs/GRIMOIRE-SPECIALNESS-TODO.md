# Grimoire Specialness Todo

Use this tracker for the work that makes Grimoire unmistakably itself. Not generic notes. Not generic AI chat. The bar is: local-first mind OS, agent-of-agents, private journal, diary, dream catcher, Markdown memory, and human-owned context.

## Definition Of Done

A task is not done until:

- [ ] the user flow works without hidden setup knowledge
- [ ] private/local-only content stays local by default
- [ ] AI actions are inspectable before durable writes happen
- [ ] Markdown on disk stays readable outside Grimoire
- [ ] tests or manual QA cover the real workflow
- [ ] docs or ADRs are updated when the abstraction changes
- [ ] `pnpm lint`, `pnpm build`, and `pnpm test` are green for touched app surfaces

## Tonight Pick List

- [x] Add the first signature dashboard: greeting, quick capture, open loops, journal/dream prompts, memory queue, recent notes, and visible locality/sync badges.
  Acceptance: Grimoire opens to the dashboard without Git and can route local captures or `/ask`.
- [x] Add vault creation templates for Blank, Journal, Dreams, Project, Research, Personal OS, Reading, People, Work Log, and Creative Studio.
  Acceptance: the create-vault modal uses template cards, storage choices, local/iCloud/GDrive paths, and sends the selected template to vault creation.
- [x] Seed created vaults with matching Type templates where the template needs them.
  Acceptance: Journal/Dream/Project/Research/Personal OS-style vaults get readable Markdown Type definitions on disk.
- [ ] Tighten the signature loop: capture -> local context -> agent suggestion -> reviewable diff -> accepted Markdown memory.
  Progress: Crystallize now exposes typed file/frontmatter/backlink/body/task/note review hunks, source-link Markdown, editable proposed Markdown, a review packet summary, a typed AI-loop write contract (`Markdown`, no Git, no remote sync, review-before-write), and `memory_status`/`memory_review_state`/`memory_source_count`/`expires_at`/`contradicted_by`/`memory_version`/`reviewed_at` metadata before writing. The proposed Markdown also includes a visible `## Ledger Contract` body section with review-by and contradiction slots, and the AI panel/review dialog show ledger contract counts, expiry, and contradiction state before apply. The AI panel also shows a Crystallize Memory loop card that makes context, Council, and review state visible before a durable write, including whether accept will create a memory note only or also patch the active note. The review dialog now adds a source-safe runway for sources, Locality Firewall state, ledger contract, editable diff, active-note target/hunk count, and local Markdown landing before apply. Dashboard `/ask` shows owner-visible protected counts in the preview, then submits a source-safe handoff package that carries public references only plus policy-only Locality Firewall state, so quick capture can move into the local context -> agent suggestion -> review path without requiring Git or hidden prompt stuffing. Daily Thread Crystallize asks now carry typed `crystallize-memory` intent through the dashboard preview, queued handoff package, and provider prompt, so downstream surfaces do not infer the review target from prose. Provider-bound prompts now also redact protected inline wikilink titles, protected contradiction labels, and exact protected graph/note omission counts. When a dashboard ask answer is crystallized, public source labels survive into `source_note`, optional `source_notes`, and Source Links; protected withheld counts stay out of written Markdown. The review dialog explicitly marks the write as local Markdown, Git-free, and review-before-write, with regression coverage that the accepted memory persists under `memory/crystallized/` inside the vault. When the editor content is available, the same review can also apply editable active-note frontmatter metadata (`last_crystallized_at`, `crystallized_memories`, and reviewer-added keys) plus an editable append through the normal local note save path. The dashboard now reads accepted Crystallize metadata as a closed loop instead of counting that Memory note as pending review again.
  Latest: the editable Memory Markdown now has an apply-time contract guard. If review edits remove required Memory Ledger/frontmatter fields, source fields, `locality: vault`, or `crystallized: true`, the dialog shows the missing contract fields and blocks durable write.
  Latest: durable Crystallize provenance now re-checks Locality Firewall at the builder boundary. Protected Dream/Journal/private active-note titles and paths are replaced with a withheld source label before Memory titles, `source_note`, or Source Links are generated.
  Acceptance: one flow demonstrates the whole loop without requiring Git, remote sync, or hidden AI memory.
- [x] Make the Locality Firewall visible enough that a user can tell what can leave before agents, exports, or sync run.
  Latest: vault-level Settings examples now show privacy-safe protected categories such as Dream note, Frontmatter-protected note, and Private folder note instead of protected note titles, keeping the proof surface aligned with the rule that private titles stay withheld.
  Acceptance: journals, dreams, private notes, and local-only vaults show explicit local-only treatment in the UI.
- [ ] Make Crystallize feel like the soul of the app, not a utility button.
  Progress: AI output now turns into a proposed Memory note with source links, confidence, frontmatter, backlinks, task preservation, editable active-note frontmatter and append hunks when the current note is writable, explicit accept/edit path, a persistent AI-panel loop card, explicit local/Git-free/no-remote review contract, a compact capture/context -> Council/firewall -> answer -> Markdown review trail, a dialog-level sources -> firewall -> editable diff -> local Markdown runway, a hunks/sources/target/tasks packet summary that names active-note write consequences before the dialog opens and again inside the dialog, editable expiry and contradiction ledger slots, an apply-time Memory Ledger contract guard, and a visible local-write accept moment instead of only a toolbar icon.
  Acceptance: AI output turns into a proposed Markdown change with source, confidence, frontmatter, backlinks, and an explicit accept path.
- [ ] Remove one high-friction UX edge that still smells like a developer-built app.
  Progress: Dashboard `/ask` now shows the public notes attached to agent context plus a protected-withheld count before send, instead of silently stuffing context into the prompt. Folder/project note-list chrome now shows the selected project scope, keeps path/metrics/actions/search in one control deck, caps noisy docs behind a counted rail, and keeps bottom file/state filters in one dock instead of scattered top/bottom chips.
  Latest: project-folder chrome now uses `docs/source` language instead of `md/other`, exposes exact metric tooltips, and renders the selected project as a breadcrumb trail so the center pane says where the notes belong without filesystem homework. The project cockpit now keeps DOM/focus order aligned with the visual search -> filters -> docs flow, labels the source toggle as project-results-only, makes hidden docs a real reveal action, and ellipsizes long breadcrumb parts instead of letting folder names break the chrome.
  Acceptance: the flow is understandable without reading docs or knowing implementation terms.
- [ ] Pick the visual identity direction for the next theme pass.
  Progress: Added `Prabhat Studio` as the next light-first full-surface preset candidate, with matching app tokens, Settings previews, sidebar artwork tuning, reduced-motion/high-contrast participation, and curated Settings placement beside Daylight Atelier.
  Acceptance: keep 2-3 strong themes, remove weak presets, and make the left rail/sidebar feel intentional in light and dark mode.

## Critical Product Gaps

- [ ] One unforgettable daily workflow.
  Problem: Grimoire has many powerful surfaces, but the daily habit loop needs to become obvious.
  Progress: Dashboard now includes a Daily Flow rail that makes the opening ritual explicit: Capture, Reflect, Organize, and Crystallize. It seeds local capture prompts, routes Organize through Attention Mode, opens `/ask` for Crystallize, shows reviewed Crystallize completion for today, and shows only source-safe counts/states instead of private dream, journal, or memory titles. The rail now has a theme-owned progress runway and finite next-step motion, so the ritual reads as one cohesive surface instead of four loose buttons. Clear review lanes say `memory clear` / `mobile clear` instead of showing noisy zero-count review chips. Dashboard `/ask` preview lists every public note reference that can travel, while protected withheld counts stay UI-local and provider-bound prompts receive only public refs plus a stable Locality Firewall policy line. Recent re-entry rows are vault-context only; protected local notes stay in their dedicated private/review lanes instead of appearing as generic recent titles. When only protected recent notes exist, Recent Notes shows a protected-held count instead of a misleading first-note prompt. Dashboard also includes Attention Mode, a quiet local next-action card that prioritizes conflicts, memory review, mobile review, open-loop drift, journaling, dreams, local changes, and closed-loop Crystallize state. It also includes the first Time Loom card, grouping recent Markdown activity, voice captures, mobile captures, vault commits, and scheduled Markdown calendar/event frontmatter by local day, type, coarse status, and private counts without exposing protected labels, audio filenames, commit messages, hashes, event details, or file paths. Latest: the lazy insight lane now opens with a Daily Thread rail that fuses Time Loom counts with Dream Forge dream/journal rhythm into one privacy-safe next action such as review private captures, close one loop, open the dream lane, or crystallize the day. The rail uses only counts, source lane labels, and local/private states; no private titles, paths, excerpts, audio filenames, device metadata, commit text, or provider claims render there. Its `Crystallize the day` action now seeds a source-safe `/ask` prompt for a Crystallize-ready Markdown memory proposal with the normal ask-context preview and a structured review-before-write intent, instead of falling back to a journal prompt.
  Target: open app, capture, reflect, organize, crystallize, and leave with cleaner memory in under two minutes.
- [ ] Vault onboarding without Git homework.
  Problem: vaults are correct, but the concept can sound like filesystem/Git setup instead of a personal space.
  Progress: Create Vault now opens with a local-first contract instead of a filesystem-first prompt: plain Markdown, private-by-default journals/dreams, and Git optional are visible before the user chooses a vault home. The modal names the destination as a Markdown folder and keeps sync framed as the local folder provider the user chooses. It also shows a creation plan before writing: template, local folder path, storage provider, Git on/off, and privacy contract. iCloud and Google Drive choices explicitly say they are still local folders and that Grimoire stores no cloud credentials. The first-run Welcome screen now says plain Markdown, local by default, and Git optional before any action, while Create Empty and Open Existing vault descriptions avoid repository language.
  Target: create/open local vault first, then optionally connect iCloud, Google Drive Desktop, Git, S3, or Azure later.
- [ ] Agent Council as an actual product surface.
  Problem: the agent-of-agents thesis is strong, but the UI needs to show separate stances, sources, conflicts, and synthesis.
  Progress: AI panel now shows first Council cards for Claude Code, Codex, local search, vault graph, Red Team, import/export context, dashboard ask context, Chitragupta, Woosh, and Tring CLI with health, permission, stance, structured claim confidence, source counts, conflict counts, source-safe contribution summaries, source-safe graph-neighborhood badges, clickable safe source badges, synthesis copy, friction signals for missing/checking/private/protected lanes, Memory Ledger conflict chips, a current-pass brief with scope/deliverable/safety posture, a compact Council map for source labels, ready lanes, friction, held-local policy, lane health, and review state, a visible one-answer resolution card with confidence/source/friction counts and the next review step, a four-step workflow rail for intake/Council/synthesis/review, a read-only synthesis packet for sources/stances/friction/workflow/next Markdown step, source-safe Red-Team next actions, and a Context Capsule/prompt handoff preview of what source-safe graph/dashboard context is included or withheld. Red-Team My Plan now participates as a Council critique lane instead of only a separate side card. Chitragupta is now shown as a private MCP-contract memory lane, not a generic available CLI. Protected ask packages cannot leak contradiction labels or exact protected counts into policy-only Council passes. The Graph modal now mirrors that contract with visible connector insight, selectable graph nodes, source-safe package HUD, selected-node detail actions, per-agent Council lanes, a source-label/held-local preview, an Agent Package manifest panel, and a Locality Firewall-guarded "Ask Council" path that re-roots the package on the selected node. Graph asks now open a review dialog before AI handoff, showing source labels, graph counts, held-local counts, edge manifest, and exact prompt text, then close the graph overlay so the queued packet is visible in the AI rail. Graph asks then carry the same source-safe package through the AI prompt queue, Council summary, and Context Capsule preview, with protected graph nodes/edges counted but not named. The graph canvas now marks Council-ready edges, selected paths, local-only held paths, source-safe package tethers, a package card, an agent-readiness rail, and a selected-node orbit for local search, vault graph, Chitragupta, Codex, and Claude Code directly on the map without exposing protected labels.
  Proof boundary: this is a product/UI scaffold with source-safe package review and synthesis state. It is not yet full live fanout across all named agents until Chitragupta recall/wiki/graph, Claude/Codex contribution capture, and live provider evidence for importer/exporter/storage lanes are verified end to end.
  Latest: graph asks now remain the active Council/capsule source in the AI rail even if another editor note is open; the graph modal closes after reviewed handoff so the AI rail is not hidden behind the overlay; canvas rail, selected-node orbit, and side-panel lane states share one Local Search / Vault Graph / Chitragupta / Codex / Claude Code lane model backed by the real `claude_code` AI route; the graph HUD and legend now reserve top/bottom rails instead of floating over nodes; zero-match graph filters show no package instead of stale active-note handoff. Graph local-only nodes now use a lock glyph instead of a developer text marker, and the node renderer is split from the canvas so future graph polish has room under the LOC guardrail. Graph lanes now say source-safe eligibility instead of pretending to prove live CLI/MCP health, Chitragupta graph copy is policy-eligible rather than capability-verified, the selected route shows CLI missing/checking/installed state beside the graph package, and the Agent Package manifest separates Locality Firewall holds from preview trimming. Locality Firewall state now wins over CLI availability for protected graph selections, while CLI missing/checking/installed remains visible as secondary route health. The selected-node orbit now carries a small secondary health bead for local/installed/checking/missing status, and the graph legend names Route health so the map shows both axes without exposing protected note labels. Council synthesis is now explicitly review-gated, with ready/private-gated/unavailable lane counts and held-local counts visible before any Markdown write, so staged Council packets do not pretend to be full live fanout.
  Latest handoff: source-safe Council synthesis packets can now open Crystallize as reviewed local Markdown Memory proposals with `source: "Agent Council"`, a Handoff Gate section, Council source labels in `source_notes`/Source Links, source-safe `handoff_*` packet metadata, and the normal editable apply path. Protected/policy-only packets expose no source labels and remain blocked instead of being smuggled into memory. Accepted Council memories show that handoff state again in the Memory Ledger without storing exact protected-held counts.
  Latest evidence: Council lanes now carry explicit source-safe evidence rows. Local Search shows the active note, ask-package references, Memory Ledger records, and linked-context evidence it can use. Vault Graph shows safe neighbor nodes and graph edge manifests such as `Source -> Target`, while protected graphs render only Locality Firewall evidence. Import/Export now shows Import Autopsy manifests, export manifests, storage proof level, and Locality Firewall holds as inspectable evidence rows without calling S3/Azure/iCloud/GDrive provider-proven. The synthesis packet also includes those evidence rows so review sees what each lane actually used instead of only stance prose.
  Latest route truth: Agent Council and graph runway surfaces now reuse the same active route disclosure as the footer. Chitragupta can show its explicit provider/model route, including Google/Gemini when configured, directly beside the source-safe or no-note-payload privacy state before any handoff. Graph Council handoff is regression-covered for missing/checking selected routes, and the graph dialog shell is covered so right-rail overflow stays contained instead of clipping review controls on shorter screens.
  Latest graph polish: selected graph nodes now draw state-colored connector rays into the Local Search, Vault Graph, Chitragupta, Codex, and Claude Code orbit lanes, so the map reads as an agent-routing surface instead of detached status dots while protected labels remain withheld.
  Target: Codex, Claude, local search, graph, importer/exporter context, and private local agent lanes can produce inspectable contributions.
- [ ] Context Capsules as inspectable agent packages.
  Problem: agents need selected notes, rules, project map, and exclusions, but the human should see the package before it leaves the UI.
  Progress: AI panel now shows a preview-only Context Capsule with included active/linked/tab notes, note-list count, source-safe graph nodes/edges, safety rules, and local-only exclusions. The capsule can open a read-only Markdown package preview with source labels, graph-neighborhood counts, rules, project-map counts, exclusions, and a handoff checklist before any agent/export/sync/write action. Dashboard `/ask` context packaging now lazy-loads only when Ask is selected or submitted, so regular local capture does not pay agent-context startup cost. Graph Council asks now generate their own capsule package with selected source labels, graph counts, and local-only exclusions before handoff, and the graph handoff itself requires a review dialog before queueing the AI prompt.
  The capsule card now shows the package route directly: gather, Locality Firewall, project map, and Markdown review, with held-local/trimmed/source counts surfaced before any handoff. Context Capsule card and review dialog now also reuse the same active agent route disclosure as the footer/Council/graph runway, so Chitragupta provider/model truth and source-safe/no-payload state are visible at the package boundary instead of hidden in chat. Daily Thread Crystallize asks now also surface their typed review-before-write Markdown-memory intent in the capsule card and Markdown package preview before any packet moves. Capsule cards and Markdown package previews now share the Inspector Firewall's agent/export-sync/Git-cloud egress matrix, so a review shows what can leave through each lane instead of only a generic handoff count.
  Locality preflight now appears in Context Capsule, Graph Council handoff, and Agent Council synthesis review dialogs, showing allowed context, held-local counts, and trimmed counts before any agent packet moves forward.
  Latest: the Context Capsule review dialog can copy the exact reviewed Markdown package to the local clipboard, with visible copied/failed/unavailable states and no agent handoff side effect. Each reviewed package now carries a privacy-safe `pkg-*` receipt derived from the sanitized Markdown package body, and the capsule card/dialog show that receipt before any handoff.
  Target: one-click capsules become portable, inspectable, and local-only safe without hidden prompt stuffing.
- [ ] Private agent lanes stay private.
  Problem: Chitragupta, Woosh, and Tring CLI are part of the agent-of-agents direction, but their private internals should not become public app assumptions.
  Progress: Chitragupta provider routing is explicit and Chitragupta-only: no override means local CLI default, and stale provider values for Codex/Claude are ignored instead of being disclosed as real routes. The AI message route chip now uses structured Grimoire-owned route state from Chitragupta stream events, with the old reasoning-text parser kept only as fallback compatibility, so route truth stays visible outside collapsed reasoning and cannot be overwritten by model self-description. The bottom-bar AI menu, Agent Council, and graph runway now show the selected route truth from app settings, including Chitragupta's stream-resolved default or explicit provider/model override, while Settings AI Agents states the same product boundary in plain UI: Chitragupta chat health is the local CLI route, while MCP memory, recall, wiki, graph, and diagnostics are separate readiness checks.
  Proof boundary: the current Grimoire-side proof is CLI route/status/error parsing. Chitragupta MCP memory, wiki, graph, ingest, and diagnostics are tracked as required contract work, not current ready behavior.
  Target: Grimoire can surface them as private/local capabilities with health, permissions, runtime provider/model route, and source-backed outputs while keeping configs, prompts, credentials, and implementation details local-only.
- [ ] Memory Ledger product depth.
  Problem: first records exist, but memory still needs edit/version/confidence/contradiction/expires workflows.
  Progress: Inspector records can now open the underlying Markdown memory, edit confidence, expiry, and contradiction metadata, then stamp `memory_version` and `reviewed_at` on save. Ledger rows now show source links, confidence tone, expiry urgency, contradiction count, reviewed/version state, and navigable source/contradiction chips without reading hidden state. The Inspector also shows a metadata-only evidence strip for record count, unique source count, contradiction count, and review flags, while local-only notes withhold the strip entirely. A new audit queue names the next expired, expiring, contradicted, stale, or unreviewed memory and opens that Markdown note through normal navigation, without reading private note bodies or exposing protected source paths.
  Target: every durable AI memory is a Markdown note with editable metadata and visible provenance.
- [ ] Locality Firewall product depth.
  Problem: backend filtering exists, but users need visible confidence before anything leaves the vault.
  Progress: Settings now shows a vault-level Locality Firewall inspector with protected/public counts, frontmatter/type/path reasons, protected examples, and agent/export/sync rules. The Inspector now shows an active-note Firewall card with metadata-only reason, agent/export-sync/Git-cloud lane states, and protected local notes withholding title/path/body/frontmatter from egress by default. That egress matrix is now a shared policy contract reused by Context Capsule review cards and Markdown package previews, keeping agent, export/sync, and Git/cloud wording consistent at every handoff boundary.
  Agent-facing review dialogs now carry a Locality Firewall preflight strip so source-safe packets are visible at the moment of handoff, not only in Settings.
  MCP vault/project tools now enforce the same no-escape rule: protected notes are withheld even if an old override-shaped option is passed.
  Target: per-vault, per-note, per-export, and per-agent policy inspectors with local-only defaults for dreams/journals/private lanes.
- [ ] Crystallize product depth.
  Problem: first reviewed memory-note creation exists, but the richer diff workflow is not finished.
  Progress: Memory-note creation, frontmatter, backlinks, body, tasks, and active-note frontmatter/append paths are now typed review hunks. The active-note hunks are editable in the review dialog and write through the existing local note save path with no Git requirement, preserving existing frontmatter while appending Crystallize memory links.
  Latest: Agent Council synthesis now enters this same review path as an `Agent Council` Memory source and carries its safe Council provenance plus structured `handoff_*` review metadata into the Memory note, so the agent-of-agents answer has a durable Markdown route without invisible AI memory, protected-count leakage, or automatic writes.
  Latest: protected active-note provenance is redacted before the durable Memory artifact is built, so Crystallize cannot smuggle Dream/Journal/private titles through generated titles or source metadata.
  Target: proposed note edits, frontmatter edits, backlinks, tasks, and new memory notes appear as reviewable hunks.
- [ ] Living Frontmatter without a hidden database.
  Problem: frontmatter can carry the mind-OS structure, but it should not become brittle schema enforcement or invisible metadata.
  Progress: Inspector now shows hints for missing fields, stale active status, possible duplicates, and wikilinks that may deserve relationship fields. Safe schema suggestions can now be applied through the normal Markdown frontmatter update path with visible suggested values; no hidden database writes are introduced. Markdown Type notes can now ask for readable fields through `required_fields`/`required`/`fields` or `_list_properties_display`, and Living Frontmatter turns those into local per-note hints instead of a hard schema.
  Target: schema guidance, relationship hygiene, and memory metadata suggestions stay inspectable, editable, and Markdown-owned.
- [ ] Dream Forge as private-only intelligence.
  Problem: dreams/journals are part of the soul, but they need their own protected surface.
  Progress: Dashboard now shows a local-only Dream Forge surface with private-surface markers, protected counts, private-safe recency, metadata-only symbols, emotional weather, recurring people, a small private signal map, and an explicit privacy contract that allows local dashboard signals while non-local reports get only counts/redaction facts. Dream Forge now also has a private rhythm rail for last-night, this-week, and older dream/journal activity using counts only, a private timeline for last-night/weekly/monthly/archive bands using record, held-local, and signal counts only, plus a theme-owned private-lens contract for record count, held-local count, and frontmatter-only signals. The Dream Forge visual layer lives in its own CSS file under the LOC guardrail, with finite compositor-safe arrival motion and no dream titles, paths, bodies, or cloud language. The dashboard now also surfaces the redacted egress report state directly: blocked egress and held signal-label counts are visible without exposing dream titles, paths, bodies, or labels. The loading fallback already says local-only/no-cloud/egress-blocked, so the lane does not briefly read as a generic card.
  Target: recurring symbols, people, emotions, time patterns, and dream timelines stay local unless the user explicitly exports.
- [ ] Time Loom as a temporal graph.
  Problem: daily notes, journals, dreams, meetings, tasks, commits, voice notes, and calendar need one time-aware surface instead of scattered recency lists.
  Progress: Dashboard now shows a metadata-only Time Loom card with local-day buckets, type counts, coarse status counts, mobile capture counts, voice counts, vault commit counts, scheduled calendar counts, due task counts, reviewed Crystallize badges, protected counts through the Locality Firewall, and a compact temporal map. Mobile capture drafts are reduced to metadata-only mobile counts before rendering, scheduled mobile captures still increment the mobile count, Task/Todo due frontmatter is reduced to `Task` counts on the due date, Git pulse data is reduced to `Commit` counts only, commits touching protected paths still raise the private count, every protected custom type label coarsens to `Private`, and calendar/event Markdown is reduced to `Calendar` counts only. Time Loom now also includes a metadata-only Pattern Lens for primary thread, open-loop pressure, and private-review pressure, derived from safe counts only and covered so titles, paths, project labels, due-key names, audio filenames, providers, device/source metadata, and raw statuses cannot leak. Latest: Time Loom feeds the Daily Thread guidance rail with count-only source lanes and next-step copy, so the temporal graph pushes one local action without turning private metadata into an agent/provider payload. Calm daily threads can now route into `/ask` for a source-safe, review-before-write Crystallize proposal.
  Target: temporal graph navigation connects private lanes, tasks, calendar, commits, and captures while protected titles/paths/bodies stay local and hidden by default.
- [ ] Attention Mode without productivity-app nagging.
  Problem: the assistant should notice drift without bossing the user around.
  Progress: Dashboard now derives one local next action from sync blockers, memory queue, pending mobile review, unresolved open-loop drift, too many active Markdown threads, repeated recent context switches, journal/dream freshness, pending local changes, reviewed Crystallize memory, and calm recent-thread states. The action is no longer a generic capture button: Memory/mobile review opens the review note, focus/task/journal/dream suggestions seed the exact local capture lane, repeated context switching becomes one "Settle thread" focus capture, calm recent-thread states seed a source-safe Crystallize ask prompt, reviewed Crystallize memory closes the loop for the day, and noisy local changes seed a local Memory note for the reason to defer sync. AI input surfaces now hide protected notes from wikilink suggestions and redact protected wikilink labels before sending.
  Target: Attention Mode should connect to the full daily loop: open item, capture, journal, dream, crystallize, or defer sync.
- [ ] Red-Team My Plan as a thinking partner.
  Problem: plans should be challenged before they become commits, sync changes, or AI memory.
  Progress: AI panel now shows a local red-team card for the active note/plan across product, code, execution, UX, privacy, and evidence, plus a review-only Markdown patch-plan preview. The preview is checklist-shaped and source-safe: no protected titles, paths, excerpts, or raw note content. The same red-team signals also feed the Agent Council as a structured source-safe critique lane, and the Council synthesis packet now derives safe next actions from known Red-Team dimensions instead of trusting raw note-derived action text.
  Target: selected notes, issues, and plans can run multi-lane critique with source-backed next actions and reviewable Markdown follow-through.
- [ ] Import/export/sync proof.
  Problem: provider lanes exist, but "works" must mean verified with real files and failure states.
  Target: Bear, Day One, Apple Journal, Obsidian, Notion, local Markdown, Markdown ZIP, iCloud Drive, Google Drive Desktop, S3, Azure, local SQLite snapshots, and pure JSON snapshots have clear support status and tested behavior.
  Product rule: iCloud Drive and Google Drive Desktop are user-owned local folders, not Grimoire cloud accounts. SQLite and JSON are portable local export/import lanes for structured backups, audits, agent packages, and future iOS/iPad handoff; they must never become the hidden source of truth.
  Progress: Markdown folder, Bear, Markdown ZIP, Apple Journal, Day One, Journey, Obsidian, Notion ZIP/folder, and Spanda imports now have no-write preview/autopsy paths that report planned notes, assets, skips, destination, and local-only report before import. Settings keeps the latest no-write preview visible as an in-memory timeline with shortened local paths, live announcements, and a source-safe manifest for files, metadata, attachments, and guarded local-only skips. Markdown/Bear folder imports now count files inside pruned local-only folders such as `.codex`, `.grimoire-local`, `mockups`, and `certs` as skipped instead of silently omitting them, and cancellable progress imports preserve the same skipped totals. Obsidian/Notion-style app imports now count hidden/runtime files such as `.obsidian`, `.vscode`, `.env`, and unsafe ZIP traversal entries as skipped in preview/import/progress reports while still refusing to copy them. Local-only attachment closure now covers Git, ZIP, static HTML, object-storage mirrors, and `grimoire-canvas` source/preview files. Markdown ZIP and static HTML exports now count files withheld from `.grimoire-local`, `.codex`, `.mcp.json`, `.env*`, and `mockups` in skipped totals instead of silently pruning them, and export toasts now say how many local-only files were withheld. Markdown ZIP export writes through a same-folder temp file and only replaces the final archive after `zip.finish()`, so cancelled progress exports keep the previous ZIP. Object-storage mirror apply now requires an exact preview signature, and Settings shows visible S3/Azure local-mirror fixture push/pull cards with conflicts and local-only withheld examples plus reasons before apply. Object-storage progress previews now report pruned local-only files such as `.grimoire-local`, `.mcp.json`, `.env*`, and `mockups` the same way non-progress previews do, so Settings cannot show protected junk as upload/download candidates. S3 now has a separate read-only live preflight through local AWS config that reports configured/reachable/denied/missing states without returning object keys, credentials, or local paths; Settings now collects bucket, region, and prefix as a transient draft so the proof is not hidden behind env-only setup. Azure Blob now has a separate read-only live preflight through local Azure CLI login that reports missing CLI/login/permission/container/network states without returning CLI output, object keys, credentials, or local paths; Settings collects account, container, and prefix as a transient draft only. iCloud Drive and Google Drive Desktop now have local-folder proof buttons that check provider path shape/readability without cloud credentials. Settings now also shows a Proof Ledger so ready importers, portable exits, desktop sync, S3/Azure read-only preflight, and S3/Azure local-mirror fixture status are visible in one place without implying live cloud SDK sync. The ledger now separates support status from proof level and lists the remaining live-provider/corpus checks directly in Settings. Latest hardening: S3/Azure provider apply now blocks conflicts, refuses stale Settings targets after preview, disables hidden environment fallback for Settings-entered drafts, requires a preview signature in mocks, redacts local paths from storage errors, skips local-only files before hashing, and uses Grimoire SHA-256 object/blob metadata so same-size divergent remote content conflicts instead of being treated as equal.
  Proof boundary: current confidence is fixture/regression backed plus local manual flows. iCloud/Google Drive are provider-managed local folders with health checks, S3 and Azure have read-only live preflight proof paths plus opt-in live write/read/delete proof harnesses, and both providers now have Settings-visible preview/apply lanes behind explicit user intent, exact preview signatures, conflict blocking, and content-hash comparison. Neither provider lane is provider-proven until real failure states and live fixture runs are recorded.
  Latest review: this is not yet "works 100%" proof. Run `pnpm test:object-storage-live` plus the Settings provider lanes against real providers/failure states before calling object storage sync ready. The live-proof runner refuses to run as proof without explicit S3/Azure gates and reports only set/missing env state. Keep expanding Import Autopsy from summary counts to exact source file -> destination file, metadata transform, attachment move, and withheld-reason manifest rows. Import reports with absolute paths stay local-only forever and UI rows must redact `/Users/...` paths. The Import Autopsy UI now renders exact manifest rows as source basenames plus vault-relative destinations only, and Markdown/Bear folder, Obsidian/Notion-style app, journal, and Markdown ZIP preview backends now emit note/asset/withheld rows for those surfaces. Sanitized Bear/TextBundle, Day One JSON, Day One media folders, Apple Journal HTML, Journey JSON, Obsidian, Notion, and mixed Markdown ZIP corpora now have golden manifests that prove Markdown/journal-note, attachment, metadata, hidden/runtime withheld, local-only file/folder, unsafe ZIP traversal rows, Day One `photos`/`videos`/`audios`/`pdfs` rows, and Obsidian `.canvas`/wiki-embed/Markdown-link/relative-attachment rows where applicable. The Obsidian link-style corpus now also runs through actual import to prove copied notes/assets/canvas, preserved wiki/Markdown link text, and withheld `.obsidian` runtime config. Mixed Markdown ZIP now also has a stress regression for large Markdown content, unsupported binary withholding, and unsafe traversal withholding.
  Latest manifest portability: Import Autopsy can now copy a redacted no-write Markdown manifest with summary buckets, exact source-basename -> vault-relative destination rows, withheld reasons, and locality contract text, while absolute `/Users/...` source paths remain local-only.
  Innovation track: add a local Portability Capsule that can emit Markdown folder, Markdown ZIP, pure JSON, and local SQLite variants from the same reviewed manifest. JSON should be human-diffable and agent-friendly; SQLite should be a local read-optimized snapshot with provenance tables, Locality Firewall withholding evidence, and reversible import proof before apply.
  Latest capsule slice: pure JSON and local SQLite exports now share one Locality Firewall inventory, write only vault-relative paths inside the artifacts, include withheld reasons and locality proof, and keep Markdown as source of truth. JSON writes pretty human-diffable files with UTF-8 text inline and binary assets base64 encoded. SQLite writes real local tables for metadata, files, withheld rows, and proof. Reversible JSON/SQLite import preview/apply now feeds Import Autopsy, restores into local `imports/` lanes, verifies hashes and byte counts, rejects traversal and sources inside the active vault, and keeps withheld/local-only rows withheld. The Settings Proof Ledger now also reflects the latest redacted S3/Azure read-only preflight reports when present, and can load a pasted redacted live proof-runner JSON report as status/gate/config evidence, separating live reachability evidence from still-unproven provider write/sync behavior.
  Latest review gate: import and capsule restore writes now stay disabled until the matching no-write Import Autopsy preview exists, and the write path reuses that reviewed source path instead of opening a second picker. JSON/SQLite capsule exports now also require a matching reviewed export preview before choosing a local target or writing, and Settings shows the reviewed export summary without source paths.
- [ ] Settings re-overhaul acceptance pass.
  Problem: Settings is now powerful, but every section must feel like one Apple-native control surface instead of a long form.
  Progress: Settings already has themed rail/main materials, compact iPad/mobile navigation, intent-gated portability lanes, local theme-pack JSON load/reload, and now first-class language choices for English, Simplified Chinese, German, Hindi, and Sanskrit. The panel now opens on Portability/local vault control first, with Sync & Updates as an optional layer, and the vault rail says Local files / Local files + Git instead of framing local storage as a lesser Git-off state. Portability now leads with the action deck before audit surfaces, keeps the no-write Import Autopsy review gate directly under the action deck, keeps Proof Ledger deep commands/setup/remaining proof behind per-row disclosure, and labels S3/Azure provider cards as proof previews with an explicit "not provider-proven sync yet" boundary.
  Target: every Settings panel uses the active theme, has clear section rhythm, no browser-default controls, no horizontal overflow, no hidden setup knowledge, and no path/provider copy that implies private files leave the machine.
- [ ] Native Mac presence.
  Problem: Grimoire should feel like a real Mac app, not a browser wearing a jacket.
  Target: menu bar icon, quick capture, Finder/Quick Look paths, global note creation, and CLI create/open/rebuild feel coherent.
  Progress: Command Palette now exposes "Reveal Note in Finder", "Reveal Vault in Finder", and "Preview Note with Quick Look"; the native commands canonicalize paths, refuse anything outside the active vault, and keep Quick Look scoped to file previews instead of vault-folder opens. The native menu-bar icon also has local quick-capture lanes for Thought, Journal, and Dream that route through the shared command catalog into the dashboard capture box instead of writing invisible notes or requiring Git.
- [ ] Native iOS and iPadOS companion.
  Problem: Grimoire must work where capture actually happens: phone, tablet, pencil, camera, voice, and offline reading.
  Target: an iPhone/iPad app that preserves the same local-first Markdown/vault contract, uses Files/iCloud document access as the default mobile storage story, respects journal/dream/private locality rules, and turns mobile capture into reviewable Markdown rather than hidden app state.
  Progress: The first mobile capture draft contract now turns iPhone/iPad quick-capture, share, voice, camera, transcript, and Pencil input into vault-relative Markdown with `locality: local`, `egress: blocked`, `mobile_review: pending`, `mobile_capture_schema: grimoire-mobile-capture-v1`, stable capture IDs, and Files-provider storage hints. Draft filenames, H1s, and frontmatter titles stay neutral until review, so private capture text is not pushed into path metadata. Dream, Journal, and Memory captures start in protected lanes and stay reviewable before they become agent context or durable memory. Each draft now also carries `review_required`, `agent_context: blocked_until_review`, `export_context: blocked_until_review`, `sync_context: local_until_review`, pending review outcome, and a visible Mobile Review checklist in Markdown. Voice, camera, transcript, and Pencil assets become local attachment manifests with safe filenames and review checkboxes while device paths and checksums stay out of note Markdown. Pending and blocked mobile captures now appear in the dashboard daily flow and Attention Mode as counts/actions only, accepted/merged/moved/discarded captures graduate out of the review queue, the queue uses privacy-safe metadata items instead of full private note records, and Time Loom folds them into metadata-only mobile counts using `captured_at` for unscheduled capture chronology, so the review loop and temporal graph are visible without exposing private mobile titles, paths, device/source values, attachment paths/checksums, or bodies.
- [ ] UI identity and theme discipline.
  Problem: visual polish is improving, but it still needs a signature look instead of many equal presets.
  Target: fewer stronger themes, better rail/sidebar artwork, no muddy icons, no weak palettes, and a modern niche 2050-ish feel.
  Progress: Constellation now uses shared surface tokens across sidebar, note list, editor, AI rail, Settings, and status bar, with CSS regression coverage against raw color islands. Theme packs now carry semantic accent/text roles so status dots, selected rows, AI reference chips, tooltips, navigator panels, graph canvas, graph handoff, and center-pane chrome stay inside the active preset. Dashboard, Daily Flow, and Dream Forge now route through dashboard/rail/private-local theme materials instead of generic card color islands. Settings now curates presets into Signature, Studio, and Lab groups so Living, Nocturne, and Constellation read as the house directions instead of competing equally with experimental presets. The weak `research-cockpit` preset and standalone `manuscript` preset are retired from the shipped catalog; Living Archive keeps the manuscript/journal feeling through its typography and material rules. The sidebar artwork has moved from a plain book/graph sketch toward a theme-tinted vault-atlas mark with book/map, memory, route, keeper, orbit, constellation, theme-lit aura, core glow, and local-memory jewel layers; the glyph display contract is covered so the mark cannot silently disappear again. Center-pane project chrome now embeds file/state filters inside the project control deck instead of leaving a separate chip strip, keeps project material on inner surfaces instead of wrapper islands, project action names stay stable while copied/saved/scanned state is exposed separately, folder/project ownership chips stay compact on cards, and graph agent package cards now use explicit graph material classes instead of substring utility-class patching.
  Latest: the bottom status bar remaps nested text/popover tokens to footer-local colors, preserves warning/success/danger action tones after generic and grouped button resets, graph package cards/rails/orbit lanes/legend marks preserve ready/guarded/blocked color states after shared material overrides, Inspector and AI panels share the same theme-local token remap, Living Archive feeds its parchment gradient into shared panel material, and Agent Council plus graph review/preflight surfaces now draw computable material from a dedicated theme layer instead of motion or utility background rules. Context Capsule card/dialog route, stat, and package-step surfaces now have explicit theme-owned material classes and CSS regression coverage instead of leaning on generic utility color islands. Settings rail, mobile rail, active section buttons, and form controls now use a dedicated theme material layer instead of ad-hoc background utilities. Durable Memory, Crystallize, and Import Autopsy motion now lives in a focused motion file below the LOC guardrail instead of bloating the shared motion token file. Built-in themes now carry UI/editor/mono/display/label typography roles in the same JSON contract as local theme packs, and font resolution applies them before user editor-font or local theme overrides. The center-pane selected note card now uses an inset aurora shimmer instead of a left-border rail, while path/code/system mono surfaces route through the local-only Grimoire Berkeley/TX-02 Berkeley Mono stack.
  Next: theme means a whole-app material system. Colors, spacing, gradients, density, typography, radius, shadows, and motion must stay coherent across sidebar, note list, editor, inspector, AI rail, Settings, dialogs, dashboard, graph, import/export, status bar, and mobile-ready surfaces.
- [ ] Production confidence.
  Problem: special does not matter if the basics wobble.
  Target: lint/build/tests stay green, core flows have smoke coverage, and import/export/locality workflows have regression coverage.
  Progress: The oversized app command catalog is split into definition, type, and facade modules below the source-file LOC guardrail. AI-agent status discovery is deferred and cached so CLI version checks do not run as immediate startup work, while explicit refresh remains immediate. Note-list data, sort, search, property, interaction, content, and model state are now split into focused files below the LOC guardrail, the list reuses one prefiltered entry pass for sort metadata and render data, selected note paths prune when visible scope changes, duplicate-basename change status is no longer guessed, Tauri CSP no longer allows blanket remote `https:` fetches, the macOS installer now refuses non-singleton Grimoire app bundles, and the dashboard route/capture planner now lazy-loads so note-first startup skips dashboard summary modules. The AI right panel now lazy-loads the full chat/Markdown stack only when opened, keeping Inspector-only note reading lighter without dropping queued prompts or chat history. Product motion now has a CSS regression gate that keeps flagship, graph, dashboard, sidebar, and Council keyframes compositor-safe, finite, and reduced-motion aware; previous box-shadow pulse animations are static glow plus opacity/transform motion. The shared motion foundation is split so `motion.css` and `motion-memory.css` both stay below 400 LOC with regression coverage.
  Latest: the oversized vault switcher hook is split into facade, shared utility, persisted state, pure action model, and action-hook modules below the 400 LOC guardrail, preserving the same local-first/Git-optional vault API and regression coverage. Tab management is now split into cache, content-error, navigation, and hook-facade modules, the release-history page builder now imports standalone CSS/script assets, note-list keyboard navigation is split into model/event/facade modules, app-save behavior is split into type, rename, persistence, and public hook modules, note creation is split into model/planning, persistence, immediate-queue, and public hook modules, the BlockNote formatting toolbar is split into controller, model, and controls modules, the Inspector relationships panel is split into panel, rows, search controls, panel model, and search model modules, the editor breadcrumb bar is split into shell, actions, filename-title editing, model, and type modules, sidebar shared parts are split into nav item, section content, visibility popover, selection, and type modules, and property value cells are split into primitive editors, display-mode selector, type definitions, and pure cell-model helpers so those behavior files stay under the LOC guardrail.

## Anti-Generic Rules

- [ ] Do not add a feature if it could belong unchanged in any notes app.
- [ ] Do not let AI write invisible memory.
- [ ] Do not let dreams, journals, private notes, or local-only files leave by default.
- [x] Do not expose private Chitragupta, Woosh, or Tring CLI internals in public docs, logs, exports, or PRs.
- [ ] Do not make Git required for creating or opening a vault.
- [ ] Do not hide Markdown behind a database-shaped abstraction.
- [ ] Do not ship cloud/provider sync without local working-copy behavior and clear failure states.
- [ ] Do not add another surface until the signature loop gets sharper.

## Parking Lot

- [x] Research and rank the exact importer edge cases for Bear, Day One, Apple Journal, Obsidian, Notion, Journey, and Markdown ZIP.
  Result: see `docs/IMPORTER-EDGE-CASE-MATRIX.md`. First P0 fix landed in the journal importer: duplicate nested media basenames now resolve by exact relative path and copy to unique attachment destinations instead of overwriting or linking the wrong file. Day One now also has sanitized golden proof for all four official media folders: `photos`, `videos`, `audios`, and `pdfs`. Obsidian now has sanitized golden proof for `.canvas`, wiki embeds, Markdown links, and relative attachment paths, plus actual import proof that the same files copy without leaking `.obsidian` config. Mixed Markdown ZIP now has large-note and unsupported-binary import proof.
- [x] Prototype Agent Council response cards with source links and disagreement handling.
- [x] Design the private-agent adapter boundary for Chitragupta, Woosh, and Tring CLI with only names, health, permissions, and outputs crossing into Grimoire.
- [x] Design the Dream Forge privacy model before adding any dream analytics UI.
- [x] Add a local-only docs/scratch audit so internal planning files do not accidentally ship when they should stay local.
- [ ] Decide whether commercial dual licensing needs a CLA before external contributions become serious.
