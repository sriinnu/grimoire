# Grimoire Product Contracts

Versioned, UI-neutral wire contracts shared by Grimoire presentation clients.

This package mirrors the serialized contracts owned by the Rust `grimoire-core`
crate. It must not contain React components, Tauri commands, SwiftUI views, or
provider-specific behavior.
