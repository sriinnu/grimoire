use crate::commands;

macro_rules! app_invoke_handler {
    () => {
        tauri::generate_handler![
            commands::list_vault,
            commands::list_vault_folders,
            commands::get_note_content,
            commands::create_note_content,
            commands::save_note_content,
            commands::update_frontmatter,
            commands::delete_frontmatter_property,
            commands::rename_note,
            commands::rename_note_filename,
            commands::move_note_to_folder,
            commands::auto_rename_untitled,
            commands::detect_renames,
            commands::update_wikilinks_for_renames,
            commands::get_file_history,
            commands::get_modified_files,
            commands::get_file_diff,
            commands::get_file_diff_at_commit,
            commands::get_vault_pulse,
            commands::git_commit,
            commands::get_build_number,
            commands::get_last_commit_info,
            commands::git_pull,
            commands::git_push,
            commands::git_remote_status,
            commands::git_add_remote,
            commands::get_conflict_files,
            commands::get_conflict_mode,
            commands::git_resolve_conflict,
            commands::git_commit_conflict_resolution,
            commands::git_discard_file,
            commands::is_git_repo,
            commands::init_git_repo,
            commands::check_claude_cli,
            commands::get_ai_agents_status,
            commands::get_ai_provider_key_statuses,
            commands::save_ai_provider_api_key,
            commands::clear_ai_provider_api_key,
            commands::get_vault_ai_guidance_status,
            commands::restore_vault_ai_guidance,
            commands::stream_claude_chat,
            commands::stream_claude_agent,
            commands::stream_ai_agent,
            commands::get_transcription_readiness,
            commands::transcribe_audio,
            commands::preview_markdown_folder_import,
            commands::preview_markdown_zip_import,
            commands::preview_portability_capsule_import,
            commands::import_markdown_folder,
            commands::import_markdown_folder_with_progress,
            commands::cancel_markdown_folder_import,
            commands::cancel_portability_operation,
            commands::import_markdown_zip,
            commands::import_portability_capsule,
            commands::import_markdown_zip_with_progress,
            commands::import_journal_export,
            commands::import_journal_export_with_progress,
            commands::preview_journal_export,
            commands::preview_app_export,
            commands::import_app_export,
            commands::import_app_export_with_progress,
            commands::export_markdown_zip,
            commands::export_markdown_zip_with_progress,
            commands::preview_portability_capsule,
            commands::export_portability_capsule,
            commands::run_portability_capsule_loop_proof,
            commands::export_static_html_archive,
            commands::export_static_html_archive_with_progress,
            commands::storage_health_check,
            commands::storage_desktop_provider_health_check,
            commands::storage_s3_live_preflight,
            commands::storage_azure_live_preflight,
            commands::storage_s3_provider_push_preview,
            commands::storage_s3_provider_pull_preview,
            commands::storage_s3_provider_sync_apply,
            commands::storage_azure_provider_push_preview,
            commands::storage_azure_provider_pull_preview,
            commands::storage_azure_provider_sync_apply,
            commands::storage_push_preview,
            commands::storage_push_preview_with_progress,
            commands::storage_pull_preview,
            commands::storage_pull_preview_with_progress,
            commands::storage_sync_apply,
            commands::storage_sync_apply_with_progress,
            commands::reload_vault,
            commands::reload_vault_with_progress,
            commands::reload_vault_entry,
            commands::sync_vault_asset_scope_for_window,
            commands::sync_note_title,
            commands::save_image,
            commands::save_audio_recording,
            commands::save_canvas_preview,
            commands::copy_image_to_vault,
            commands::delete_note,
            commands::batch_delete_notes,
            commands::batch_delete_notes_async,
            commands::migrate_is_a_to_type,
            commands::create_vault_folder,
            commands::rename_vault_folder,
            commands::delete_vault_folder,
            commands::batch_archive_notes,
            commands::get_settings,
            commands::check_for_app_update,
            commands::update_menu_state,
            commands::trigger_menu_command,
            commands::update_current_window_min_size,
            commands::perform_current_window_titlebar_double_click,
            commands::reveal_path_in_finder,
            commands::preview_path_with_quick_look,
            commands::save_settings,
            commands::download_and_install_app_update,
            commands::load_vault_list,
            commands::save_vault_list,
            commands::git_clone::clone_git_repo,
            commands::search_vault,
            commands::create_empty_vault,
            commands::create_getting_started_vault,
            commands::check_vault_exists,
            commands::get_default_vault_path,
            commands::register_mcp_tools,
            commands::remove_mcp_tools,
            commands::check_mcp_status,
            commands::repair_vault,
            commands::reinit_telemetry,
            commands::list_views,
            commands::save_view_cmd,
            commands::delete_view_cmd
        ]
    };
}

pub(crate) fn with_invoke_handler(
    builder: tauri::Builder<tauri::Wry>,
) -> tauri::Builder<tauri::Wry> {
    builder.invoke_handler(app_invoke_handler!())
}
